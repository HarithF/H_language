;
int x;
char y;
struct Z z;
//int;              // leads to segmentation Fault...        
//struct Z;         //leads to segmentation Fault...

void f(int x){
    //int;
    //int a b;
    x+3*;
    while (true) {
        break a;
    }
    //void (*((*(*p))(int))); 
    //void ***p(int);

    ((((((4+3))))));

    void *((*(*p))(int));
    void *((*(*p)));
    void *(*(*a)(int));

    void *(p);
    void (*(*(*(p(int)))));
    void (*p)(int x);
    void (*((*(p)(int))));
    void *((*(p)(int)));
    void (*((*(*p))(int)));
    void *(*(*(p)(int)));
    s.x++;
    *a;
    *s.x;
    return -(*&s.x = sizeof(s.x)*(p->x-y)); 
    //true ? 1 : 2;
    a = true ? 1+2 : a.z;
    a || true ? 1*b : s||o;
    true ? 1 : 2 || 3;

    struct S {int x; ;; char c;} s;

    a||b=c;
}

;;
;
;

void main(int x) {
    ;;;
    1+2;
    a.b++;
    !a.b;

    int d; 
    int (*d);
}